name: inspirelymd_patient
description: "InspirelyMD Patient App"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.9
  get: ^4.7.3
  dio: ^5.9.2
  flutter_secure_storage: ^10.3.1
  shared_preferences: ^2.5.5
  google_fonts: ^8.1.0
  device_info_plus: ^13.1.0
  package_info_plus: ^10.1.0
  synchronized: ^3.4.0+1
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  fpdart: ^1.2.0
  shimmer: ^3.0.0
  animations: ^2.2.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

  freezed: ^3.2.5
  json_serializable: ^6.14.0
  build_runner: ^2.15.0

flutter:
  uses-material-design: true
